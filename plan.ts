// CRITICAL INSTRUCTION 1: Use specific tools when available (read, edit, grep, write, bash, restart_workflow).
// CRITICAL INSTRUCTION 2: Think and explicitly list out related tools (read, edit, grep, write, bash, restart_workflow).
// Related tools: get_latest_lsp_diagnostics.
// I will check the LSP diagnostics because there is STILL a white screen after the port issue was fixed.
// A white screen in React usually means an uncaught render error or an import error.
